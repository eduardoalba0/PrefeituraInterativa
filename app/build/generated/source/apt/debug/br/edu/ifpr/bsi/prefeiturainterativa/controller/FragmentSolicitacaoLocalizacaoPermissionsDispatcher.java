// This file was generated by PermissionsDispatcher. Do not modify!
package br.edu.ifpr.bsi.prefeiturainterativa.controller;

import androidx.annotation.NonNull;
import java.lang.Override;
import java.lang.String;
import java.lang.ref.WeakReference;
import permissions.dispatcher.PermissionRequest;
import permissions.dispatcher.PermissionUtils;

final class FragmentSolicitacaoLocalizacaoPermissionsDispatcher {
  private static final int REQUEST_UPDATELOCATION = 1;

  private static final String[] PERMISSION_UPDATELOCATION = new String[] {"android.permission.ACCESS_COARSE_LOCATION","android.permission.ACCESS_FINE_LOCATION"};

  private FragmentSolicitacaoLocalizacaoPermissionsDispatcher() {
  }

  static void updateLocationWithPermissionCheck(@NonNull FragmentSolicitacaoLocalizacao target) {
    if (PermissionUtils.hasSelfPermissions(target.requireActivity(), PERMISSION_UPDATELOCATION)) {
      target.updateLocation();
    } else {
      if (PermissionUtils.shouldShowRequestPermissionRationale(target, PERMISSION_UPDATELOCATION)) {
        target.showRationale(new FragmentSolicitacaoLocalizacaoUpdateLocationPermissionRequest(target));
      } else {
        target.requestPermissions(PERMISSION_UPDATELOCATION, REQUEST_UPDATELOCATION);
      }
    }
  }

  static void onRequestPermissionsResult(@NonNull FragmentSolicitacaoLocalizacao target,
      int requestCode, int[] grantResults) {
    switch (requestCode) {
      case REQUEST_UPDATELOCATION:
      if (PermissionUtils.verifyPermissions(grantResults)) {
        target.updateLocation();
      } else {
        if (!PermissionUtils.shouldShowRequestPermissionRationale(target, PERMISSION_UPDATELOCATION)) {
          target.onNeverAskAgain();
        } else {
          target.onPermissionDenied();
        }
      }
      break;
      default:
      break;
    }
  }

  private static final class FragmentSolicitacaoLocalizacaoUpdateLocationPermissionRequest implements PermissionRequest {
    private final WeakReference<FragmentSolicitacaoLocalizacao> weakTarget;

    private FragmentSolicitacaoLocalizacaoUpdateLocationPermissionRequest(
        @NonNull FragmentSolicitacaoLocalizacao target) {
      this.weakTarget = new WeakReference<FragmentSolicitacaoLocalizacao>(target);
    }

    @Override
    public void proceed() {
      FragmentSolicitacaoLocalizacao target = weakTarget.get();
      if (target == null) return;
      target.requestPermissions(PERMISSION_UPDATELOCATION, REQUEST_UPDATELOCATION);
    }

    @Override
    public void cancel() {
      FragmentSolicitacaoLocalizacao target = weakTarget.get();
      if (target == null) return;
      target.onPermissionDenied();
    }
  }
}
