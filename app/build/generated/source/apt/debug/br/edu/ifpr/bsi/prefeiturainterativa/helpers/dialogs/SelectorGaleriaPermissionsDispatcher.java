// This file was generated by PermissionsDispatcher. Do not modify!
package br.edu.ifpr.bsi.prefeiturainterativa.helpers.dialogs;

import androidx.annotation.NonNull;
import java.lang.Override;
import java.lang.String;
import java.lang.ref.WeakReference;
import permissions.dispatcher.PermissionRequest;
import permissions.dispatcher.PermissionUtils;

final class SelectorGaleriaPermissionsDispatcher {
  private static final int REQUEST_INITGALERIA = 7;

  private static final String[] PERMISSION_INITGALERIA = new String[] {"android.permission.WRITE_EXTERNAL_STORAGE","android.permission.READ_EXTERNAL_STORAGE"};

  private SelectorGaleriaPermissionsDispatcher() {
  }

  static void initGaleriaWithPermissionCheck(@NonNull SelectorGaleria target) {
    if (PermissionUtils.hasSelfPermissions(target.requireActivity(), PERMISSION_INITGALERIA)) {
      target.initGaleria();
    } else {
      if (PermissionUtils.shouldShowRequestPermissionRationale(target, PERMISSION_INITGALERIA)) {
        target.showRationale(new SelectorGaleriaInitGaleriaPermissionRequest(target));
      } else {
        target.requestPermissions(PERMISSION_INITGALERIA, REQUEST_INITGALERIA);
      }
    }
  }

  static void onRequestPermissionsResult(@NonNull SelectorGaleria target, int requestCode,
      int[] grantResults) {
    switch (requestCode) {
      case REQUEST_INITGALERIA:
      if (PermissionUtils.verifyPermissions(grantResults)) {
        target.initGaleria();
      } else {
        if (!PermissionUtils.shouldShowRequestPermissionRationale(target, PERMISSION_INITGALERIA)) {
          target.onNeverAskAgain();
        } else {
          target.onPermissionDenied();
        }
      }
      break;
      default:
      break;
    }
  }

  private static final class SelectorGaleriaInitGaleriaPermissionRequest implements PermissionRequest {
    private final WeakReference<SelectorGaleria> weakTarget;

    private SelectorGaleriaInitGaleriaPermissionRequest(@NonNull SelectorGaleria target) {
      this.weakTarget = new WeakReference<SelectorGaleria>(target);
    }

    @Override
    public void proceed() {
      SelectorGaleria target = weakTarget.get();
      if (target == null) return;
      target.requestPermissions(PERMISSION_INITGALERIA, REQUEST_INITGALERIA);
    }

    @Override
    public void cancel() {
      SelectorGaleria target = weakTarget.get();
      if (target == null) return;
      target.onPermissionDenied();
    }
  }
}
