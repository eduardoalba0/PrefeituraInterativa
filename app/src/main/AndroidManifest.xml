<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="br.edu.ifpr.bsi.prefeiturainterativa">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".controller.AdmAtendimentoListarActivity"></activity>
        <activity android:name=".controller.BaseActivity" />
        <activity android:name=".controller.AdmAtendimentoActivity" />
        <activity android:name=".controller.SolicitacaoVisualizarActivity" />
        <activity android:name=".controller.SolicitacaoListarActivity" />
        <activity android:name=".controller.SolicitacaoCadastroActivity" />
        <activity android:name=".controller.UsuarioCadastroActivity" />
        <activity android:name=".controller.UsuarioLoginActivity" />
        <activity android:name=".controller.ActivityMain" />
        <activity android:name=".controller.ActivitySplash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <uses-library
            android:name="org.apache.http.legacy"
            android:required="false" />
    </application>

</manifest>